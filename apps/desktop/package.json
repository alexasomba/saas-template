{
  "name": "desktop",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "vp run web#build && vpx electrobun dev",
    "dev": "vpx electrobun dev --watch",
    "dev:hmr": "concurrently \"vp run hmr\" \"vp run start\"",
    "hmr": "vp run --filter web dev",
    "build": "vp run web#build && vpx electrobun build",
    "build:stable": "vp run web#build && vpx electrobun build --env=stable",
    "build:canary": "vp run web#build && vpx electrobun build --env=canary",
    "check-types": "vpx tsc --noEmit"
  },
  "dependencies": {
    "electrobun": "^1.15.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "concurrently": "^9.2.1",
    "typescript": "catalog:"
  }
}
